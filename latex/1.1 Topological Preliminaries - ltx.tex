\documentclass[11pt]{article}
\usepackage{amsmath, amssymb, amsthm}
\usepackage[margin=1in, top=1.2in, bottom=1.2in]{geometry}
\usepackage{parskip}
\usepackage[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue]{hyperref}
\usepackage{cleveref}
\theoremstyle{definition}
\newtheorem{definition}{Definition}[section]
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}{Lemma}[section]
\newtheorem{proposition}{Proposition}[section]
\newtheorem{corollary}{Corollary}[section]
\theoremstyle{remark}
\newtheorem{remark}{Remark}[section]
\newtheorem{example}{Example}[section]
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}
\begin{document}

\begin{center}
{\Large\textbf{1.1 Topological Preliminaries}}\\
\vspace{0.3cm}
{\normalsize\today}
\end{center}
\vspace{0.5cm}


% --- Page 1 ---
% Topological Preliminaries

* A \underline{topology} $T$ in a set $X$ is a collection of subsets of $X$ (called \underline{open sets}) satisfying the following axioms:

  (1) the union of any number of open sets is open

  (2) the intersection of any finite number of open sets is open.

  (3) $X$, $\emptyset$ are open.

*A \underline{topological space} is a set $X$ with a topology $T$ in $X$.

$T = \{ \emptyset, X \}$ is \underline{trivial topology}

$T = \mathcal{P}(X)$ is \underline{discrete topology}

* For $A \subseteq X$, where $T$ is a topology in $X$. Then $T_A$ is an \underline{induced topology} in $A$

% --- Page 2 ---
\[ T_A = \bigcap \{ U \setminus A \mid U \in \mathcal{T} \} \]

* In a topological spaces, a closed set is a complement of an open set.

* For \( E \subseteq X \), \( \overline{E} \) is said to be closure of \( E \), where \( \overline{E} = \bigcap V \), \( S \) is the set of all closed sets containing \( E \).

* A neighborhood of \( x \in X \), is any open set containing \( x \).

* A mapping \( f \) from \( X_1 \) to \( X_2 \) (\( X_1 \xrightarrow{f} X_2 \)) is an assignment of each \( x \in X_1 \) of an element \( f(x) \in X_2 \) where \( X_1, X_2 \) are topological spaces. (map and mapping are not the same, mapping is a function between two topological spaces).

% --- Page 3 ---
\[
\text{* The mapping } f \text{ is continuous if and only if for every open set } O_2 \text{ in } X_2, \text{ the set } f^{-1}(O_2) \text{ is an open set in } X_1. \text{ (note } f^{-1} \text{ need not be a mapping)}
\]

\[
\text{* For } x \in X_1, \text{ we say that } f \text{ is continuous at } x \text{, if to every neighborhood } U \text{ of } f(x), \text{ there exists a neighborhood } V \text{ of } x, \text{ such that } f(V) \subset U.
\]

\[
\text{(} f \text{ is continuous at every point of } X_1 \text{.)}
\]

\[
\text{We can show the equivalence of both definitions.}
\]

\[
\begin{lemma}
\label{lemma:composition}
\text{Composition of continuous maps are continuous.}
\end{lemma}
\]

\[
\begin{proof}
\label{proof:composition}
\text{If } f: X \rightarrow Y, g: Y \rightarrow Z \text{ are continuous.}
\]

\[
\text{Goal 1: } (g \circ f)^{-1}(V) \text{ is open in } X \text{ where } V \text{ is any open set in } Z.
\]

\[
\text{Goal 2: } (g \circ f)^{-1}(V) = f^{-1} \circ g^{-1}(V)
\end{proof}
\]

% --- Page 4 ---
\begin{example}
(c i) Every mapping is continuous from $X$ to $Y$ if $X$ has discrete topology. (I showed converse is NOT true).

(c ii) If $X$ is a topological space, $Y$ a set with trivial topology, $f: X \rightarrow Y$ is continuous.

$*$ Topological product of $X_1, X_2$ is $X = X_1 \times X_2$ where topology in $X$ is generated by a basis
$\mathcal{B} = \{O_1 \times O_2 \mid O_1 \in \tau_1, O_2 \in \tau_2 \}$.

(Def$^n$ of basis add to notes, I'm not writing here)

$*$ If $p_1: X_1 \times X_2 \rightarrow X_1$ and
$p_2: X_1 \times X_2 \rightarrow X_2$ are such that
$p_1(x_1 \times x_2) = x_1$, and $p_2(x_1 \times x_2) = x_2$ resp.
Then $p_1, p_2$ are projections of $X_1 \times X_2$.
\end{example}

% --- Page 5 ---
\begin{lemma}
Projections are continuous, open mappings.
\end{lemma}

\begin{proof}
It suffice to show for $p_1$, and for $p_2$ is the same argument.

For open map, observe every open set in $X$ can be written as $U \times V$ where $U$ is an open set in $X_1$, and $V$ is an open set in $X_2$.

Now, $p_1(U \times V) = U$.

For continuity, observe $p_1^{-1}(U) = U \times X_2$ where $U \subset X_1$ is open.

\end{proof}

\begin{remark}
$\ast$ A family of subsets $\{V_\alpha\}_{\alpha \in I}$ is a \underline{covering} if $\bigcup_{\alpha \in I} V_\alpha = X$. If each $V_\alpha$ is an open subset of $X$, $\{V_\alpha\}_{\alpha \in I}$ is an \underline{open covering} and if $|I| < +\infty$ then $\{V_\alpha\}_{\alpha \in I}$ is a \underline{finite covering}.
\end{remark}

% --- Page 6 ---
\begin{itemize}
\item A topological space $X$ is \textbf{compact}, if every open covering of $X$ contains a finite covering. A subset $A$ is said to be compact, if $A$ is compact in the induced topology.

\begin{remark}
    \begin{enumerate}
        \item $\mathbb{R}$ is not compact
        \item If $A \subseteq X$, and $|A| < +\infty$, then $A$ is compact.
        \item Let $a, b \in \mathbb{R}$, $a \leq b$ then $[a, b]$ is compact in $\mathbb{R}$.
    \end{enumerate}
\end{remark}

\begin{proof}
    refer to theorem 27.1 and corollary 27.2 of Munkres.
\end{proof}

Let $X$ be a simply ordered set having least upper bound property. Then every closed interval in $X$ is compact. (Check out Prahlad sir's notes on General Topology for alternate path)
\end{itemize}

% --- Page 7 ---
Given an open covering of \([a, b]\) say, \(A\).

STEP 1: For \(x \in [a, b]\) there exists \(y \in [a, b]\) and \(y > x\) such that \([a, y]\) is covered by at most two elements of \(A\). (Do case where immediate successor exist and immediate successor doesn't exist).

STEP 2: \(C = \{ y \in [a, b] \mid [a, y] \text{ are covered by finite elements of } A \}\)

Observe \(C \neq \emptyset\) by applying STEP 1 for \(x = a\) and apply least upper bound property where \(c = \sup C\).

STEP 3: Show \(c \in C\)

STEP 4: \(b = C\).

% --- Page 8 ---
% Finite intersection property : In topological space
% X, let $\mathcal{F}_{\lambda}$ be collection of closed sets. If
% intersection of any finite subcollection of $\mathcal{F}_{\lambda}$ is non-empty.
% Then $\bigcap_{\lambda} F_{\lambda} \neq \emptyset$.

% A space holds FIT iff the space is cpt.

\begin{theorem}
\label{thm:closed_subset_compact}
A closed subset of a compact set is compact
\end{theorem}

\begin{proof}
Let X be compact, and Y be a closed subset of X.

For any open covering of Y, say $\mathcal{A}$, we construct $\mathcal{A} \cup \{X \setminus Y\}$ as an open covering of X as $X \setminus Y$ is open. As X is compact, let $\mathcal{A}'$ be a finite subcover of X. Now, the finite subcover of Y we get from $\mathcal{A}'$ will not contain $X \setminus Y$, hence finite subcover of $\mathcal{A}$.
\end{proof}

% --- Page 9 ---
\begin{itemize}
    \item \begin{theorem}
    \label{thm:continuous_image_compact}
    A continuous image of a compact set is compact.
    \end{theorem}
    \begin{proof}
    \checkmark
    \end{proof}

    \item \begin{theorem}
    \label{thm:compact_hausdorff_closed}
    If $X$ is compact and $Y$ is Hausdorff and $f: X \rightarrow Y$ is continuous, then $f(X)$ is closed in $Y$.
    \end{theorem}
    \begin{proof}
    \checkmark
    \end{proof}

    \item \begin{definition}
    \label{def:locally_compact}
    A space $X$ is locally compact, if for every $x \in X$, tcompact_locally_compact}
    Every compact space is locally compact.
    \end{theorem}
    \begin{proof}
    \checkmark
    \end{proof}

    \item Converse may not be true. \begin{example}
    \label{ex:IR_not_compact}
    Example: $\mathbb{R}$
    \end{example}
    \begin{proof}
    $\bigcirc$ $\mathbb{R}$ is not compact
    \end{proof}
\end{itemize}

% --- Page 10 ---
For $x \in \mathbb{R}$, there exists $(x-\varepsilon, x+\varepsilon)$
as a neighborhood of $x$ and $(\overline{x-\varepsilon, x+\varepsilon}) = [x-\varepsilon, x+\varepsilon]$
is compact.

* Homeomorphism is a mapping between
two topological spaces $X, Y$ say $f$ such
that $f$ is bijective map where $f, f^{-1}$ are
continuous mappings.

\begin{example}
Add from C. Chandrashekaran.
\end{example}

% --- Page 11 ---
Note: All topological spaces are $T_1$, moving forward unless mentioned otherwise.

* Two sets $M_1$ and $M_2$ are separated by open sets if there exist open sets $O_1$ and $O_2$ containing $M_1$ and $M_2$ such that $O_1 \cap O_2 = \emptyset$.

* Two sets $M_1$ and $M_2$ are separated by a real function if there exists a continuous real function $f: X \rightarrow [0, 1]$ such that $f(x) = 0$ for $x \in M_1$ and $f(x) = 1$ for $x \in M_2$.

$T_1$: For any disjoint point, there exists an open set containing one and not the other. Equivalently, all singletons are closed.

$T_2$ (Hausdorff): Two distinct points can be separated by open sets.

$T_3$ (Regular): For closed set $F$ and $x \notin F$, can be separated by open sets.

$T_4$ (Normal): Disjoint closed sets can be separated.

% --- Page 12 ---
\begin{itemize}
\item by open sets.
\end{itemize}

\begin{remark}
$T_4 \Rightarrow T_3 \Rightarrow T_2 \Rightarrow T_1$.
\end{remark}

\begin{example}
From the book.
\end{example}

\begin{lemma}
\textbf{Urysohn's lemma}

In a normal space, every two disjoint closed sets can be separated by a real function.
\end{lemma}

\begin{theorem}
\textbf{Tychanoff Theorem}

Product of compact sets are compact

There are two approaches of proving this theorem
\begin{enumerate}
\item Zorn's lemma
\item Transfinite Induction
\end{enumerate}
\end{theorem}

% --- Page 13 ---
% blank page

\end{document}